APP_NAME=Media Traffic AI Analyst
APP_ENV=dev
APP_DEBUG=true

# Opcional (quando vazio, o client tenta inferir automaticamente)
GCP_PROJECT_ID=

# Caminho relativo ao root do projeto (ou absoluto, se preferir)
GOOGLE_APPLICATION_CREDENTIALS=credentials/google.json

# Chave do provedor LLM (sera usada nas fases seguintes)
OPENAI_API_KEY=your-openai-key
